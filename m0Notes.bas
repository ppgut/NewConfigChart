Attribute VB_Name = "m0Notes"
'not in use - to develope new functionality:
'for now OldCC data is processed on a spreadsheet - it could be loaded to memory and processed as an array to speed up the program
'
'Private Sub ImportOldConfChart()
'
'    Dim OldCCarr() As Variant
'    OldCCarr = Range(Sheet1.Cells(1, 1), Sheet1.Cells(65000, 7).End(xlUp)).Value
'
'End Sub
